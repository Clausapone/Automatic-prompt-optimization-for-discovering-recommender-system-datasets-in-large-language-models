# GEPA for Detecting Memorization in LLM-based Recommender Systems

## 📌 Overview
This research investigates the phenomenon of dataset memorization in Large Language Models (LLMs) within the context of recommender systems. Specifically, we employ the **GEPA (Genetic Pareto Algorithm)** as an evolutionary optimization strategy to systematically probe whether LLMs memorize training data rather than generalize from it.

The work builds upon two prior studies:
1. *Do LLMs Memorize Recommendation Datasets? A Preliminary Study on MovieLens-1M*
2. *Exploring Approaches for Detecting Memorization of Recommender System Data in Large Language Models*

Our contribution extends these efforts by framing memorization detection as a **multi-objective optimization problem**, where GEPA evolves prompts designed to extract latent memorized information from models.

---

## 🧠 Introduction
Large language models (LLMs) have been widely adopted for many machine learning tasks. However, a growing concern is that these models may rely on memorizing datasets encountered during training rather than learning generalizable patterns. Dataset memorization can lead to misleading experimental evaluations because strong performance may stem from data leakage rather than true generalization.

In this study, we present an evolutionary-based methodology using GEPA (Genetic Pareto Algorithm) to investigate dataset memorization in LLMs. Our approach uses GEPA to automatically generate input queries and prompt structures that maximize the extraction of potentially memorized information. Framing memorization detection as a search and optimization problem, GEPA explores a space of candidate expressions designed to elicit specific data instances, attributes, or relationships from the target dataset.

We evaluate memorization by measuring the overlap between the information generated by the model and the original dataset using coverage and similarity-based metrics. The evolutionary process discovers non-trivial query patterns that would be difficult to design manually and provides a systematic, model-agnostic probing mechanism. Our experimental results show that GEPA effectively reveals varying degrees of dataset memorization across different models and configurations.

Overall, this study introduces GEPA as a novel, automated tool for auditing memorization in large models and contributes to more reliable evaluation practices and a deeper understanding of how training data influences model behavior.

---

## 📚 Table of Contents
- [Overview](#-overview)
- [Introduction](#-introduction)
- [Project Structure](#-project-structure)
- [Prerequisites](#-prerequisites)
- [Installation & Setup](#-installation--setup)
- [Usage](#-usage)
- [Methodology](#-methodology)
- [Evaluation Metrics](#-evaluation-metrics)
- [Results](#-results)
- [Future Work](#-future-work)
- [Contributing](#-contributing)
- [License](#-license)

---

## 📂 Project Structure

```text
gepa/
├── .gitignore                  <-- Git configuration
├── pyproject.toml              <-- Project configuration (
├── uv.lock                     <-- Lockfile for reproducible builds
├── requirements.txt            <-- Project dependencies
├── README.md                   <-- Main documentation (This File)
├── CITATION.cff                <-- CFF configuration for citing the project
├── CONTRIBUTING.md             <-- Contribution guidelines
├── LICENSE                     <-- Repository's License
│
├── src/                        <-- MAIN SOURCE CODE
│   ├── configuration.yaml      <-- Configuration parameters for Hugging Face and LLM models
│   ├── gepa_optimization.py    <-- Script responsible for running GEPA optimization processes
│   ├── llm_optimized_request.py<-- Generic handler making LLM interactions optimized requests
│   ├── llm_optimized_request_items.py <-- Handling target metrics & LLM item interactions
│   ├── llm_optimized_request_users.py <-- Handling target metrics & LLM user interactions
│   ├── book_crossing.py        <-- Testing environment scripts specific to the BookCrossing dataset
│   └── results_computation.py  <-- Simple pipeline element for generating combined results
│
├── datasets/                   <-- FOLDER 
|
│   ├── README.md               <-- Documentation explaining models, sources, and shapes of datasets utilized
│   ├── movielens100k_*.csv     <-- MovieLens 100K datasets
│   ├── movielens1M_*.csv       <-- MovieLens 1M datasets
│   └── bookcrossing_*.csv      <-- BookCrossing datasets
│
├── utils/                      <-- CORE UTILITIES & ALGORITHMS
│   ├── gepa.py                 <-- Core GEPA (Genetic Pareto Algorithm) implementation
│   └── gepa_utils.py           <-- Utility functions mapped against GEPA's operations
│
├── tests/                      <-- METRICS & TESTING
│   ├── test_metrics.py         <-- Scripts containing routines to check similarities, fuzzy matches, and more metrics
│   ├── test_replacements.py    <-- Utility validations modifying replacement scripts / testing csv alterations
│   └── test_special_prompt.py  <-- Sandbox validating explicit prompt behaviors and metrics testing
│
├── prompts_collection/         <-- GENERATED PROMPTS STORAGE
│   └── ...                     <-- Repositories persisting prompts dynamically mapped by GEPA
│
├── results_csv/                <-- OUTPUT FOLDERS
│   └── ...                     <-- Compiled statistics and memorized interactions written to format 
│
└── assets/                     <-- STATIC RESOURCES
    └── ...                     <-- System architecture diagrams or visualizations
```

---

## ⚙️ Prerequisites
Before running the project, ensure you have the following installed:

- Python 3.9+
- pip or conda
- Virtual environment tool (venv / conda)
- GPU support (optional but recommended)

### Required Python Libraries
- numpy
- pandas
- scikit-learn
- torch (or other backend depending on model)
- transformers
- tqdm
- matplotlib / seaborn

---

## 🚀 Installation & Setup
```bash
# Clone the repository
git clone <repository-url>
cd <repository-name>

# Create virtual environment
python -m venv venv
source venv/bin/activate   # On Windows: venv\Scripts\activate

# Install dependencies
pip install -r requirements.txt
```

---

## ⚙️ Methodology

The core idea is to treat memorization detection as a search problem:

- **Search Space**: Prompt structures and query templates
- **Optimization**: Multi-objective (Pareto-based)
- **Objectives may include**:
    - Maximizing dataset coverage
    - Maximizing similarity to ground truth
    - Minimizing prompt complexity (optional)

**GEPA evolves prompts through:**
- Mutation
- Crossover
- Selection based on Pareto dominance

---

## 📏 Evaluation Metrics

We evaluate memorization using:
- **Exact Match Rate** – Direct overlap with dataset entries
- **Similarity Metrics** – Semantic similarity (e.g., cosine similarity or fuzzy string matching)
- **Coverage** – Fraction of dataset correctly recovered under prompts
- **Diversity** of Extracted Data

---

## 📊 Results

Key findings include:
- **GEPA** can uncover non-trivial memorization patterns embedded directly within the weights.
- **Different models exhibit varying memorization behaviors** influenced by context length and instruction biases.
- **Prompt evolution significantly outperforms manual probing** across a spectrum of evaluation configurations.

*(Insert plots, tables, or summaries here)*

---

## 🔮 Future Work
- Extend to larger and more diverse datasets
- Explore defenses against memorization
- Apply to multimodal models
- Integrate privacy-preserving training techniques

---

## 🤝 Contributing

Contributions are welcome. Please:
1. Fork the repository
2. Create a new branch
3. Submit a pull request

## 📄 License

This project is licensed under the MIT License.