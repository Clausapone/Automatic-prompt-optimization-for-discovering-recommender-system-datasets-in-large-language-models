{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File (Debug Libs)",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "justMyCode": false,
            "python": "${workspaceFolder}/venv/bin/python"
        }
    ]
}